# Suites include file-backed ORM, migration and recovery workflows.
# This bounds the whole suite; tests for locks/process/HTTP deadlines assert
# their own shorter limits. The WFL runner also owns a hard child timeout.
timeout_seconds = 180
logging_enabled = false
debug_report_enabled = false
