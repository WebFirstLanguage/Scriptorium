# WFL currently caps the whole CLI invocation at 300 seconds.
# Each child suite also has the runner's separate bounded wait.
timeout_seconds = 300
execution_logging = false
debug_report_enabled = false
allow_shell_execution = true
shell_execution_mode = sanitized
kill_on_shutdown = true
max_buffer_size_bytes = 8388608
